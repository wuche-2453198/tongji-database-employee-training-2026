# 风险台账

> 维护人：黄茁宇。每次站会更新状态、责任人和下一次检查日期；风险关闭必须附验证证据。
> 状态取值：`OPEN`（未触发/已知悉）、`IN_PROGRESS`（处置中）、`CLOSED`（已关闭并附证据）。
> 2026-09-09 按代码、分支和 PR 实况回写一次；完整盘点见 [项目现状盘点（2026-09-09）](项目现状盘点-20260909.md)。下次检查：2026-09-10 站会。

| ID | 风险 | 触发信号 | 等级 | 主责 | 应对措施 | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | Oracle 云端安装、监听或端口不可用 | D1 无远程连接 | P0 | 钮培源、王天宇 | 本地 Oracle 先验证迁移/API；D2 前确定云机替代方案 | **CLOSED**：阿里云 ECS + Oracle Free 23ai 可用，`V001`~`V003`、`S001`~`S003` 已执行，`/api/health/db` 真实连接通过 |
| R-002 | 字段、枚举和接口三端不一致 | Swagger、DDL、页面字段不同 | P0 | 黄茁宇、王天宇、彭浩 | 开发期允许调整接口；进入联调或模块验收前必须统一 DDL、DTO/Swagger、前端类型和测试 | **IN_PROGRESS**：前端分支落后 `develop` 113 个提交、仅调用 6 个路径；组织模块使用 `/api/blacklists`、`/api/department-trainings`、`DELETE /api/employees/{id}` 等偏离基线且未登记 |
| R-003 | 报名并发超卖或重复 | 最后一个名额出现两个成功 | P0 | 苏黄罡、王天宇 | 课程行锁、事务和函数唯一索引；必须有并发用例 | OPEN：报名分支已实现容量校验与冲突用例（84 项测试含容量场景），但未合并、未跑真库并发用例 |
| R-004 | 未授权跨部门审批或访问 | 主管可操作其他部门数据 | P0 | 刘昊诺、王天宇 | 角色外再校验当前用户部门和资源归属 | **IN_PROGRESS**：PR #11 的员工/部门/黑名单三个 Controller 仅标 `[Authorize]`，任何登录用户可增删改，须补策略或角色限制 |
| R-005 | 演示数据被联调污染 | 主链路无法复现 | P1 | 钮培源 | D3/D8/D12/D14 备份 baseline，D14 冻结 demo-ready | OPEN：尚无 baseline 记录 |
| R-006 | 多人跨模块直接修改导致回归 | 非所有者修改内部代码 | P1 | 黄茁宇 | 跨模块只通过公开契约，改动需模块负责人确认 | **IN_PROGRESS**：成果评估模块由非负责人（黄茁宇）整改合入；报名模块自建 SQL 直接 JOIN `EMPLOYEES`/`BLACKLIST`，与组织模块公开 Service 形成重复资格逻辑 |
| R-007 | 业务实现与最终文档脱节 | 接口已联调但 Swagger、页面映射或用例仍缺失 | P1 | 黄茁宇 | 允许各模块立即开发；文档不一致时不通过模块验收或最终合并 | **IN_PROGRESS**：2026-09-09 已回写看板与风险台账、迁移错位 `.http` 文件、更新测试说明；页面-接口映射表、`employees/results/demo-flow` 用例仍缺 |
| R-008 | 密码、连接串或云机凭证进入 Git | 扫描或 PR 发现敏感信息 | P2（延后） | 黄茁宇、钮培源 | 内部开发仓库，不阻塞当前开发；**最终集成版本前**统一脱敏为占位符并轮换口令 | OPEN（已知悉）：`database/oracle/README.md` 明文含 `TRAINING_OWNER`/`TRAINING_APP` 口令与公网/私网 IP。按负责人决定延后到最终集成版本处理，期间不新增明文凭据 |
| R-009 | 时间不足导致主链路不完整 | D8 前申请备案未打通 | P1 | 黄茁宇 | 冻结 P2，优先完成登录、课程、申请、报名、签到、证书 | OPEN：《项目执行与人员分工》的 D1~D15 相对排期已与实际（07-06 起至 09-08）脱节，需改为带日期里程碑 |
| R-010 | 课程发布无法执行，主链路断在第一环 | 发布接口固定返回 409 | P0 | 李君硕、朱林瀚、王天宇 | 由王天宇定义事务内预算占用契约，朱林瀚实现，李君硕在发布事务中调用 | OPEN：`CourseService.PublishAsync` 明确抛 409；`IDepartmentTrainingService` 只有 CRUD，无预算占用方法 |
| R-011 | 无 CI、无分支保护，门禁依赖人工 | 任意分支可直接推 `develop` | P1 | 黄茁宇、王天宇 | 增加最小 CI（build + `tests/backend` + 结构校验）并保护 `develop`/`main` | OPEN：`.github/` 下无任何 workflow；`main`/`develop` 均 `protected=false` |
| R-012 | 组织模块合并质量不达标 | PR #11 缺测试、授权过宽、主键非 identity | P1 | 朱林瀚、王天宇 | 评审时要求补 HTTP/单测、补策略授权、改用 identity 生成主键 | OPEN：PR #11 目前 0 评审 0 评论 |
| R-013 | 报名模块未走评审流程 | 分支存在但无 PR | P1 | 苏黄罡 | 尽快提 PR 并说明变更范围、验证结果和影响 | OPEN：`feature/module-registration` 领先 `develop` 4 个提交，无 PR |
| R-014 | 前端工程与后端契约漂移 | 合并后页面调用失败或字段不符 | P1 | 李司翰、彭浩 | 合并前产出契约对齐清单（路径、字段、枚举、分页、401/403）并逐项验证 | OPEN：`frontend-integration` 落后 `develop` 113 个提交，仅覆盖 auth/courses/training-requests/registrations |
