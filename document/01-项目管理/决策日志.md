# 决策日志

> 维护人：黄茁宇。决策状态使用 `PROPOSED`、`ACCEPTED`、`SUPERSEDED`。`ACCEPTED` 项才可作为编码依据；变更必须新增一条记录，不得覆盖历史决策。

| ID | 决策 | 状态 | 确认人 | 影响范围 | 说明 |
| --- | --- | --- | --- | --- | --- |
| D-001 | 后端采用 .NET 8、ASP.NET Core Web API、Dapper、Oracle.ManagedDataAccess.Core | ACCEPTED | 黄茁宇、王天宇、钮培源 | 后端、数据库 | 技术基线 |
| D-002 | 前端采用 Vue 3、TypeScript、Vite、Vue Router、Pinia、Axios、Element Plus | ACCEPTED | 黄茁宇、李司翰、彭浩 | 前端 | 前端基础基线 |
| D-003 | 认证采用 JWT Bearer；首期退出登录只清除客户端 Token | ACCEPTED | 王天宇、李司翰 | 认证、前端 | 不实现伪安全的服务端 logout |
| D-004 | Oracle 最低 19c，推荐 Free 23ai/XE 21c；字符集 AL32UTF8，长度语义 CHAR | ACCEPTED | 钮培源、王天宇 | 数据库、后端 | 运行时区统一 Asia/Shanghai |
| D-005 | 数据库主键使用 identity，不使用每表 Sequence 或主键触发器 | ACCEPTED | 钮培源、王天宇 | 数据库、后端 | 身份列种子不得显式写 ID |
| D-006 | 迁移账号为 `TRAINING_OWNER`，运行账号为 `TRAINING_APP` | ACCEPTED | 钮培源、王天宇 | 数据库、部署 | 应用连接后设置 `CURRENT_SCHEMA` |
| D-007 | 本期保留 13 张表，课程为一课一主讲师、一次报名一次签到 | ACCEPTED | 黄茁宇、李君硕、苏黄罡、李桐 | 数据库、业务 | 多讲师和真实扫码延期 |
| D-008 | 员工使用 `DEPT_ID`，登录使用小写 `LOGIN_NAME` 和 BCrypt `PASSWORD_HASH` | ACCEPTED | 朱林瀚、王天宇、钮培源 | 组织、认证、数据库 | 不存明文密码 |
| D-009 | 所有状态存英文枚举；是否值统一为 `Y/N` | ACCEPTED | 王天宇、全体模块负责人 | 全部模块 | 前端集中映射中文 |
| D-010 | 报名必须关联同员工、同课程的 `HR_FILED` 申请；取消后可使用原申请重新报名 | ACCEPTED | 刘昊诺、苏黄罡、钮培源 | 申请、报名、数据库 | 服务端从申请派生员工和课程 |
| D-011 | 课程发布占用部门预算；发布后冻结主讲师、部门、时间、容量、学时和预算 | ACCEPTED | 李君硕、朱林瀚、王天宇 | 课程、组织、数据库 | 通过 Service 事务处理 |
| D-012 | PRE 在备案后且开课前录入，POST/评分/证书以完成培训为前置条件 | ACCEPTED | 李桐、刘昊诺、苏黄罡 | 成果、申请、报名 | 需要统一校验时点 |
| D-013 | 管理员维护基础数据；角色为种子只读，不做角色 CRUD | ACCEPTED | 黄茁宇、王天宇 | 权限、前端 | 保留角色查询能力 |
| D-014 | 缺勤不自动加入黑名单，由 HR/管理员显式处理 | ACCEPTED | 朱林瀚、苏黄罡 | 组织、报名 | 避免隐式跨模块副作用 |
| D-015 | 项目分工中的 API 路径、DTO 名称、接口拆分和页面映射为推荐基线，不阻塞模块开发 | ACCEPTED | 黄茁宇、全体模块负责人 | 后端、前端、测试 | 模块可合理调整；最终验收前必须同步 Swagger/OpenAPI、测试和相关文档 |
| D-016 | 黑名单权限口径：读取为部门主管（限本部门）/HR/管理员；新增为管理员/部门主管（限本部门）；更新与删除仅管理员 | ACCEPTED | 黄茁宇、朱林瀚、彭浩 | 权限、组织 | 固化 2026-09-11 已实现的权限矩阵；候选名单同时排除管理员/HR/主管等特权账号，且主管范围由服务端强制注入，不信任前端传参 |
| D-017 | HR 不获得黑名单写权限，黑名单保持“HR 只读、管理员与部门主管可写”的现状 | ACCEPTED | 黄茁宇、朱林瀚、彭浩 | 权限、组织 | 曾有提案建议 HR 获得新增/解除权限，**经确认不采纳**：一律以当前产品实现为准。实际产品行为为——HR 可查询列表/详情/候选名单，但无任何写入口（前端不渲染操作按钮，后端 `POST` 为 `AdminOrManager`、`PUT`/`DELETE` 为 `AdminOnly`，均不含 HR） |

## 确认规则

1. 黄茁宇在启动会逐项确认决策；涉及模块的负责人必须明确同意或提出替代方案。
2. 决策一旦设为 `ACCEPTED`，即作为编码依据。接口细节可按 D-015 调整，关联的 DDL、DTO、Swagger/OpenAPI、前端类型和测试用例在模块最终验收前同步。
3. 已进入联调的决策如需改变，先登记替代决策和迁移/兼容方案，再修改代码。

> 2026-08-01：项目负责人确认 D-001～D-015 全部通过。
> 2026-09-11：D-016、D-017 随黑名单候选名单接口与权限调整一并登记并通过。黑名单权限一律以当前产品实现为准：D-014 中“由 HR/管理员显式处理”的执行主体，在权限层面收窄为**管理员与部门主管**（HR 只读）；D-014 其余内容（缺勤不自动加入黑名单、必须显式处理、避免隐式跨模块副作用）继续有效。D-014 原文按规则不予修改。
