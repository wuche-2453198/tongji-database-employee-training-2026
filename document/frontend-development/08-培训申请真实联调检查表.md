# 培训申请真实联调检查表

> 状态：未执行。本表仅根据当前 Controller、DTO 与前端适配代码生成；取得运行中的 API、数据库和测试账号后逐项填写“实际验证结果”，不得以 Mock 或单元测试替代。

## 执行前置

- API 基址：`________________`；环境/数据库：`________________`；执行人/日期：`________________`。
- 准备员工、部门主管、HR、管理员四类账号和跨部门样本；记录各自 Token，但不得写入仓库。
- 选定一门已发布、尚未开始且员工未持有活跃申请的课程 `courseId=________`；记录创建返回的 `requestId=________`。
- 所有成功响应均应是 `ApiEnvelopeDto`：`success`、`message`、`data`（可选 `traceId`）。申请对象应含 `id`、`employeeId`、`deptId`、`courseId`、`requestReason`、`status`、`deptApproveComment`、`hrFileComment`、`createTime`；列表 `data` 另含 `items/page/pageSize/total`。

## 用例

| 编号 | 使用角色 | 请求方法与路径 | 请求体 | 预期状态码与响应字段 | 前端页面预期行为 | 实际验证结果 |
| --- | --- | --- | --- | --- | --- |
| TR-01 创建申请 | 员工 | `POST /api/training-requests` | `{"courseId":<futureCourseId>,"requestReason":"用于岗位能力提升"}` | `201`；`data.id` 非空、`status=PENDING`、`createTime`、`requestReason` 正确 | 创建页成功提示并进入/刷新新申请详情；不得请求 `undefined` ID | 待执行：状态码____；ID____；字段/页面____ |
| TR-02 查看本人申请 | 员工 | `GET /api/training-requests/my?page=1&pageSize=20&status=PENDING` | 无 | `200`；分页和仅本人 `items`，每项含 `id/createTime/deptId` | 我的申请页显示真实列表、申请时间和状态；空列表显示业务空态 | 待执行：状态码____；范围/页面____ |
| TR-03 查看申请详情 | 创建人员工 | `GET /api/training-requests/{requestId}` | 无 | `200`；申请对象字段完整 | 详情页显示理由、创建时间、部门审批/HR备案意见；空值显示等待状态 | 待执行：状态码____；字段/页面____ |
| TR-04 主管待审批列表 | 本部门主管 | `GET /api/training-requests?page=1&pageSize=20&status=PENDING` | 无 | `200`；仅本部门待审批项与分页字段 | 部门审批页只显示本部门 PENDING 项 | 待执行：状态码____；范围/页面____ |
| TR-05 主管批准 | 本部门主管 | `PATCH /api/training-requests/{requestId}/dept-approve` | `{"comment":"批准，注意协调排期"}` | `200`；`status=DEPT_APPROVED`、`deptApproveComment` 正确 | 确认弹窗关闭，列表刷新，状态和部门意见更新 | 待执行：状态码____；字段/页面____ |
| TR-06 主管驳回 | 本部门主管；使用另一条 PENDING 申请 | `PATCH /api/training-requests/{otherPendingId}/dept-reject` | `{"comment":"当前排期不支持"}` | `200`；`status=DEPT_REJECTED`、`deptApproveComment` 正确 | 确认弹窗关闭，列表刷新，状态与意见更新 | 待执行：状态码____；字段/页面____ |
| TR-07 HR 备案 | HR | `PATCH /api/training-requests/{requestId}/hr-file` | `{"comment":"备案通过，资料完整"}` | `200`；`status=HR_FILED`、`hrFileComment` 正确 | HR备案页关闭弹窗并刷新；详情显示备案意见 | 待执行：状态码____；字段/页面____ |
| TR-08 越权访问 | 员工、跨部门主管、非 HR | 员工：`GET /api/training-requests`；跨部门主管：`PATCH /api/training-requests/{otherDeptId}/dept-approve`；非 HR：`PATCH /api/training-requests/{requestId}/hr-file` | PATCH 使用 `{"comment":"越权验证"}` | 均为 `403`，错误包络含可读 `message` 与可追踪字段（若服务端提供） | 显示 403 状态页/错误提示；不更新本地列表或详情 | 待执行：各请求状态码____；页面____ |
| TR-09 不存在 ID | 有权限主管或 HR | `GET /api/training-requests/99999`；`PATCH /api/training-requests/99999/dept-approve` | PATCH：`{"comment":"不存在的申请"}` | 均为 `404` | 详情或动作显示未找到；不留下假成功状态 | 待执行：各请求状态码____；页面____ |
| TR-10 重复操作与冲突 | 员工、主管、HR | 重复 `POST /api/training-requests`；重复批准或备案相同 ID | 创建：`{"courseId":<sameCourseId>,"requestReason":"重复申请"}`；动作：`{"comment":"重复操作"}` | 重复创建为 `409`；状态机重复批准/备案当前契约为 `400`；记录任何与预期不同的 `409` | 显示后端业务错误，刷新查询最终状态；不盲目重试写操作 | 待执行：创建____；批准/备案____；页面____ |
| TR-11 未登录 | 无 Token | `POST /api/training-requests` | `{"courseId":<futureCourseId>,"requestReason":"未登录验证"}` | `401` | 清理会话并跳转登录；不提交为成功 | 待执行：状态码____；页面____ |
| TR-12 无权限 | 已登录普通员工 | `GET /api/training-requests?page=1&pageSize=20` | 无 | `403` | 路由/接口错误边界显示无权限；不泄露列表内容 | 待执行：状态码____；页面____ |

## 结论与缺陷记录

| 项目 | 结果 |
| --- | --- |
| 创建、列表、详情、审批、驳回、备案主链路 | 待执行 |
| 401/403/404/400/409 行为 | 待执行 |
| `id`、`deptId`、`createTime`、审批/备案意见映射 | 待执行 |
| 数据范围与四角色权限 | 待执行 |
| 待修缺陷、响应样例和 `traceId` | 待填写 |
