import type { App } from 'vue'
import {
  ArrowDown,
  ArrowLeft,
  ArrowRight,
  ArrowUp,
  Calendar,
  Checked,
  Clock,
  Close,
  Collection,
  DataAnalysis,
  Document,
  Edit,
  Expand,
  Fold,
  HomeFilled,
  InfoFilled,
  List,
  Loading,
  Location,
  Medal,
  Money,
  OfficeBuilding,
  Reading,
  Refresh,
  Search,
  Select,
  Setting,
  Stamp,
  Star,
  User,
  UserFilled,
  Warning,
} from '@element-plus/icons-vue'

/** 仅注册实际使用到的图标，避免全量引入导致主包体积过大 */
const icons = {
  ArrowDown,
  ArrowLeft,
  ArrowRight,
  ArrowUp,
  Calendar,
  Checked,
  Clock,
  Close,
  Collection,
  DataAnalysis,
  Document,
  Edit,
  Expand,
  Fold,
  HomeFilled,
  InfoFilled,
  List,
  Loading,
  Location,
  Medal,
  Money,
  OfficeBuilding,
  Reading,
  Refresh,
  Search,
  Select,
  Setting,
  Stamp,
  Star,
  User,
  UserFilled,
  Warning,
}

export function registerIcons(app: App): void {
  for (const [name, component] of Object.entries(icons)) {
    app.component(name, component)
  }
}
