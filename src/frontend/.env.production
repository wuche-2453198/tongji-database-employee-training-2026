# 生产构建模式（`pnpm build:prod` = vite build --mode production）
#
# 默认按「同源部署」配置：前端静态文件与后端 API 由同一个 Nginx 暴露，
# 页面用 /api/... 相对路径请求，因此不存在跨域，也不需要配置后端 CORS。
#
# 若采用前后端分离部署（前端与 API 不同源），把 VITE_API_BASE_URL 改成
# 后端对外地址（例如 https://api.example.com），并在后端
# Cors:AllowedOrigins 中加入前端的正式域名，否则浏览器会拦截跨域请求。
VITE_APP_ENV=production
VITE_API_BASE_URL=/
VITE_USE_MOCK=false
VITE_REQUEST_TIMEOUT_MS=10000
